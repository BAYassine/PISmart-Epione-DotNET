@model IEnumerable<Presentation.Models.AppointmentVM>
@using Presentation.Models
@{
    ViewBag.Title = "PreviousAppointment";
}

<script type="text/javascript">
    var dialogue_title = document.title;
    dialogue_title = "oumayma";
    alert('aaa'+c);
</script>
<div class="tab-content">
    <div role="tabpanel" class="tab-pane active" id="orders">
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Doctor </th>
                        <th>Address</th>
                        <th>Date</th>
                        <th>Reason</th>
                        <th>Message</th>
                        <th>State</th>
                    </tr>
                </thead>
                <tbody>
                    @if (ViewBag.previousAppointment != null)
                    {
                        foreach (var item in ViewBag.previousAppointment)
                        {

                            <tr>

                                <td>
                                    @item.doctor.name

                                </td>
                                <td>
                                    @item.doctor.address

                                </td>
                                <td>
                                    @item.date_start
                                </td>

                                <td> @item.reason.name</td>

                                <td>@item.message</td>

                                <td>@item.state</td>

                            </tr>
                        }
                    }
                    else
                    {
                        <tr> No appointments.</tr>
                    }





                </tbody>
            </table>
        </div>
    </div>
</div>
